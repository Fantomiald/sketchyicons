// @sketchyicons/react-native: the same components over react-native-svg.
//
// Nothing differs from the react target but the factory each generated file
// imports, which is why adding a framework is a file this size.

import { componentTarget } from './component.mjs';

export default componentTarget({ name: 'react-native', package: 'packages/react-native' });
